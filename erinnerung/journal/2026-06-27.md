# Journal 2026-06-27

## [00:30] architekt · feat/dtb-48-adr-failsafe
- Was/Wo: **DTB-48 — ADR „Fail-safe als Multi-Layer-Architektur" (E-40) erstellt, reviewed, als eigenständiges Dokument abgelegt.** DTB-48 ist ein reiner **ADR-/Doku-Task** (kein Code; der Fail-safe-Code existiert bereits in DTB-13/DTB-28/DTB-38/DTB-64).
  - **ID-Kollision vermieden:** Ticket sah „E-39" vor, aber E-39 ist inzwischen vom Audit-Log (DTB-29) belegt → nächste freie ID **E-40**.
  - **Inhalt:** 6 Fail-safe-Schichten benannt — Ingest/Stale, Sensor-Fault, Plausibilität, **Storage/DB-Ausfall (→ 503/`unknown`)**, Assessment-Kaskade (→ ORANGE/GELB, E-34), Serve-Zeit-Re-Check. Bezug NF-01/E-34/E-35/E-31/E-36.
  - **Ablage (auf Wunsch Lucas):** eigenständiges Dokument `02-Arbeitsdokumente/ADR-E40-Failsafe-Multi-Layer.md`; Eintrag im zentralen Logbuch auf kurzen Index-Verweis reduziert.
  - **2 Review-LOWs** eingearbeitet: Titel präzisiert (Schicht 5 liefert ORANGE/GELB, nicht `unknown`), E-36-Querverweis ergänzt.
  - **Jira DTB-48:** Summary → E-40, Status → „In Arbeit", Ablage-Kommentar.
- Commit/Push: `f0661b2` "docs(adr): E-40 als eigenstaendiges ADR-Dokument (DTB-48)" (+ `c0fda7c`, `1f2c533`) → `origin/feat/dtb-48-adr-failsafe`, **PR #109**.
- Nächster Schritt: PR #109 reviewen/mergen → DTB-48 auf „Erledigt"; optional Fail-safe-**Integrationstest** je Schicht (DB-Ausfall → 503/`unknown`, Stale → `unknown`) als eigener Task **DTB-49** anlegen (im ADR als offen genannt).
  —architekt

## [02:35] architekt · Johannes Petzold (feat/dtb-27-alarm-generierung → main)
- Was/Wo: **DTB-27 (Alarm-Generierung + Severity + Hysterese) abgeschlossen & nach `main` gemergt (PR #107).**
  Die Alarm-Module (Severity-Mapping, On-Delay-Hysterese, Anzeige-Rückstufung, save-only-Persistenz, Audit,
  NF-01-Recovery) sind in den AssessmentService-Zyklus **verdrahtet** (`run_assessment_cycle`, DTB-64-Naht):
  pro Poll → assess_reading → Alarm-Generierung. Mitgenommen: P0-a (`poll_interval_s` in Config + drei
  NF-05-Cross-Checks: max_gap≥on_delay, max_gap≥stale_timeout, poll≤max_gap).
  - **Review:** mehrere externe Runden + 10-Dimensionen-Audit, alle konvergiert. AuditError zu eigenständiger
    Exception (kein Ordering-Footgun), Save+Audit-Recovery fangen jeden Fehler, max_continuity_gap_s 120→150
    (=stale_timeout+poll), Runtime frozen, Monotonie-Clamp. Begründet abgelehnt: on_delay≤stale_timeout
    (empirisch widerlegt), breites save()-Catch (Repo-Konvention: enge Trias, Bugs sichtbar).
  - **Status:** 501 passed / 16 skipped, Alarm/Assessment/Config 100 % Cov, ruff check + format sauber.
    main.py-Konflikt DTB-27↔DTB-43 aufgelöst (koexistieren). Jira **DTB-27 → Erledigt**.
- Commit/Push: Merge `15ea127` (PR #107); Branch-HEAD `768245f`.
- Nächster Schritt: Doku-PR `docs/dtb-27-entscheidungslog` (Entscheidungslog inkl. NF-09 + Backend-Konzept §4/§7)
  mergen; Folge-Tickets **DTB-66** (driving_factor/explanation) + **DTB-68** (CI ruff-format-Gate); Operator-`.txt`
  (beenden-60s-Fenster) an G3/Nick. ⚠️ Pre-Prod: G1-HTTP-Default auf HTTPS umstellen (Env, s. stand.md).
  —architekt

## [22:48] backend-dev · Luca Ganter (feat/dtb-20-defekt-erkennung + docs/ganter-entscheidungslog-dtb20)
- Was/Wo: **DTB-20 (Sensor-Defekt/Flatline) — Implementierung fertig, aber Design-Konflikt mit paralleler PR #120 entdeckt → Code NICHT gepusht, in Koordination.**
  - **Mein Ansatz:** Flatline aus `check_plausibility` herausgezogen in neue **fenster-/spannweitenbasierte** `check_flatline` (`failsafe.py`) + im Poller pro Sensor verdrahtet (`poller.py`). Grund: Single-Point-Vergleich erreicht bei 30-s-Polling nie die 15-min-Dauer + ist dither-anfällig (2 santa-loop-CRITICALs geschlossen, ε=0,15 = DS18B20 ~2×LSB).
  - **Konflikt:** DTB-20 wurde **parallel über PR #120** (`fix/dtb-20-flatline-epsilon-ds18b20`, bereits auf main gemergt) anders gelöst — reine ε-Kalibrierung + Dither-Regressionstest auf der bestehenden DTB-13-`check_plausibility`-Logik. Mein Refactor bricht deren gemergten Test `test_check_plausibility_lsb_dither_is_flatline`. Außerdem: `check_plausibility` ist auf main **gar nicht im Poller verdrahtet** (Flatline/Sprung laufen im Poll-Pfad nicht).
  - **Entscheidung Option 1 (mit Lucas beraten):** `flatline_timeout_min` bei **15** lassen (nicht auf 30) — sicherheitsrelevant: kürzeres Fenster erkennt klemmenden Sensor (falsches GRÜN bei frischen Timestamps) doppelt so schnell; deckt sich mit `Schwellenwerte.md §3`. Fehlalarm bei Schleich-Drift < 0,6 °C/h bewusst akzeptiert (fail-safe-Richtung).
  - **Tuning-Ticket DTB-69** angelegt (Lucas zugewiesen) + **G1-Anfrage** (Sensortyp/Auflösung/Rauschen) als Kommentar.
- Commit/Push: Log `f226e03` "docs(entscheidungslog): DTB-20 Flatline-Fenster + Option-1 (timeout=15) — Ganter" → **gepusht auf `origin/docs/ganter-entscheidungslog-dtb20`**. Code-Branch `feat/dtb-20-defekt-erkennung` (`7aaedd6`, mit main gemergt, grün außer dem #120-Test) **bewusst NICHT weiter gepusht**.
- Nächster Schritt: Mit **Lucas + #120-Autor** klären, welches Flatline-Design gewinnt (mein Fenster vs. mains Single-Point) — erst dann DTB-20-Code-Branch finalisieren/PR. Log-Branch kann als Doku-PR gemergt werden.
  —backenddev

## [23:10] backend-dev · Luca Ganter (Session-Abschluss DTB-20)
- Was/Wo: **Koordination aufgelöst — mein Fenster-Design gewinnt.** Lucas Vöhringer hat den Branch
  `feat/dtb-20-defekt-erkennung` übernommen und mein `check_flatline`-Fenster-Design **behalten**; den von
  PR #120 stammenden Dither-Regressionstest hat er von `check_plausibility` auf `check_flatline` **migriert**
  (Commit `49fc0ca`, **PR #124** `fix/dtb-20-dither-test-migration`, gemergt `36192be`). Der zuvor rote Test
  ist damit auf die neue Logik umgezogen.
- Stand: `feat/dtb-20-defekt-erkennung` = 1 hinter / 5 vor `origin/main` → Arbeit fertig, **finaler Merge-PR
  feat→main steht noch aus**. Entscheidungslog-Branch `docs/ganter-entscheidungslog-dtb20` separat (unverändert).
- Nächster Schritt: finalen DTB-20 feat→main-Merge abwarten/durchziehen; Entscheidungslog-Koordinationsvermerk
  bei Gelegenheit auf „aufgelöst (Design bestätigt, PR #124)" nachziehen. **Session beendet.**
  —backenddev
