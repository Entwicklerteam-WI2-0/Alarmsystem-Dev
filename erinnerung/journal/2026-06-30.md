# Journal 2026-06-30

## [08:37] architekt · Johannes (docs/g1-kontextfelder-entscheidung)
- Was/Wo: **G1-Kontextfelder Wind + Oberflächenfeuchte — Live-Snapshot (Contract v1.2)** gebaut und gemergt.
  `GET /v1/assessment/current` liefert jetzt additiv `wind_speed_ms` + `surface_moisture_pct` aus dem aktuellen
  Reading (Gut-Pfad), im Fail-safe-Zweig (stale/fault → unknown) genullt wie die Messwerte (NF-01). Umsetzung
  minimal-invasiv über `build_assessment_current` (reading ist im Builder verfügbar) — **kein** Eingriff in
  Assessment-Modell/DB/Persistenz. `AssessmentCurrent`-Schema + `openapi.yaml` + `API_FROZEN_v1` (v1.2). TDD:
  3 Unit + 1 Endpoint-Test; volle Suite grün, ruff clean.
- Commit/Push: `8abad53` "feat(api): Kontextfelder (Wind + Oberflaechenfeuchte) im Live-Snapshot assessment/current (Contract v1.2)" → **PR #169 gemergt** in `main` (mit Konfliktauflösung gegen den parallelen Wire-Change #168). In `origin/main` verifiziert (Felder + Fail-safe-Nullung intakt).
- **Doppelarbeit erkannt + bereinigt:** Mein zuerst gebauter Branch `feat/wind-speed-context-field` (Wind als
  Kontextfeld) war **redundant** — das Team hatte Wind **+ Moisture** bereits via **PR #164 (Contract v1.1)**
  gemergt (Ingest/Persistenz/Historie). Konsequenz: redundanten Branch verworfen (nicht gemergt); nur den
  echten, in main fehlenden Live-Snapshot-Gap gebaut (v1.2) + die zu #164 **fehlende** Entscheidungsdoku
  nachgezogen.
- Doku: **E-44 konsolidiert** auf `docs/g1-kontextfelder-entscheidung` (Commit `5c2dbdc`): ADR-E44-G1-Kontextfelder
  (Volltext, v1.1 + v1.2, verworfene Alternativen, Zuschreibung), Index-Zeile im zentralen Logbuch,
  Backend-Konzept §9.1. Branch auf aktuelles `main` nachgezogen (Merge `e331e82`, konfliktfrei), PR-Diff = nur
  die 3 Doku-Dateien. Persönlicher `Petzold-Entscheidungslog`: 2 neue Einträge (v1.2-Design + Doppelarbeits-Lesson).
- Nächster Schritt: (1) **Doku-PR** `docs/g1-kontextfelder-entscheidung` erstellen + mergen (mergebar, doc-only).
  (2) **2 tote Branches löschen** (`feat/wind-speed-context-field`, `docs/wind-entscheidungslog-adr`) — Safety-
  Classifier blockte die Remote-Löschung, braucht expliziten User-Lauf (`! git push origin --delete ...`).
  (3) **G3-Sign-off (Nick)** für v1.2 — versandfertige `.txt` auf dem Desktop.

## [22:45] architekt · main (Lucas)
- Was/Wo: **Testprotokoll (P5.3 / DTB-30) erstellt + Live-E2E-Durchlauf gefahren.** Neue Datei
  `04-Source-code/docs/TESTPROTOKOLL.md` (Abnahme-Checkliste, 89 Zeilen, **alle ✅**) + Desktop-Übergabeordner
  `Testprotokoll-G2-2026-06-30\` (Protokoll, Feature-Liste für die techn. Komplettvorstellung, README,
  3 Coverage-/Test-Belege). Live gegen vollen Stack (portable MariaDB 11.4.7 + G1-Sim + Backend, Scheduler scharf):
  alle 4 Ampelstufen, **beide dokumentierten Vorfälle** (V1 kein Fehlalarm, V2 Eis trotz +1,2 °C Luft),
  Alarm + On-Delay 60 s, Ack 200 / Double-Ack 409, Audit-Trail (inkl. `reading_ingested` → F11-Fix live bestätigt),
  Fail-safe NF-01 (STALE/FAULT/G1-DOWN → unknown bzw. 503, nie GRÜN), alle `/v1`-Endpunkte (readings, audit,
  thresholds GET+POST mit Auth, CORS, ack-Fehlerfälle). Belege LV1–LV20 in §3a.
- Coverage live gemessen: `assessment/` **100 %**, gesamt `src/` **97 %** (mit DB); Suite **892 passed / 0 skipped**.
- Commit/Push: **keiner** — Testprotokoll liegt **uncommitted** in `04-Source-code/docs/` (+ Desktop-Kopie außerhalb
  Repo). Test-Artefakt `_be.log` entfernt.
- Status-Modell-Korrektur (User-Feedback): jede bestandene Zeile trägt **✅** statt irreführend „n/a"/„⏳";
  Spalte „Live" → „Status", Zusatz = Nachweisart (LIVE / auto / STOA-RB / 🔒). Persönlicher Entscheidungslog: 1 Eintrag.
- Nächster Schritt: (1) **Unterschriften** Test-Team (Arezo/Amelie) + Architekt. (2) **Jira DTB-30** auf erledigt.
  (3) optional Testprotokoll auf Feature-Branch committen + PR (Freigabe Lucas).
—architekt
