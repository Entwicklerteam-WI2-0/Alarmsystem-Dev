# Journal 2026-06-22

## [14:18] architekt · Johannes (feat/ci-base)
- Was/Wo: CI/CD-Setup (DTB-11) — `.github/workflows/test.yml` + `requirements.txt`/`requirements-dev.txt` (Laufzeit/Dev getrennt, Versionen `~=` gepinnt, pip-Cache, timeout-minutes, permissions: contents read). Drei Review-Runden eingearbeitet (alle LOW/INFO). Branch wird **auf Eis gelegt**: voller Abschluss hängt an DTB-1 (Scaffolding `src/`+Tests), weil die CI sonst nicht grün wird und die DoD-Schutzregel `main` blockieren würde.
- Git/PR: e4b0c32 "ci: Test-Workflow (GitHub Actions) und Requirements fuer T0-Stack" auf `feat/ci-base`; **PR #18** offen & mergebar. PR #15 geschlossen; verwaiste Branches `feat/ci-cd-setup`, `feat/ci-pipeline` am Remote (Cleanup → Lucas).
- Jira: DTB-11 Statuskommentar gesetzt (fertig / offen / Abhängigkeit DTB-1).
- Workflow-Notiz: Ruleset blockt Push-Korrekturen auf Feature-Branches (nur Neuanlage erlaubt) → Empfehlung an Lucas, Ruleset auf `main` zu beschränken.
- Nächster Schritt: DTB-1 / Scaffolding → danach PR #18 mergen (Lucas, §7) + Branch-Protection „Require status checks" (Check `test`) aktivieren.
- —architekt

## [15:39] architekt · docs/mysql-vorgabe-einarbeiten
- Was/Wo: Surprise-Vorgabe der Geschäftsleitung (MySQL-Datenhaltung,
  `02-Arbeitsdokumente/Surprise Anforderungen.txt`) in die Architektur eingearbeitet.
  **Backend-Konzept.md:** §6 Tech-Stack auf **MySQL/MariaDB** (dev = prod via Docker-Compose,
  SQLAlchemy + Repository-Pattern, Alembic) umgestellt; neue **§6a** mit der GL-geforderten Analyse
  (Alternativen/Vor-Nachteile/Auswirkungen/Risiken, als KI-Entwurf markiert); §4 DB-Typen + §7 Struktur
  (`migrations/`, `docker-compose.yml`). **README.md:** Scope, Tech-Stack-Tabelle, Deliverable-Beschreibung,
  Schnelleinstieg (`docker compose up -d db`) gespiegelt.
- Entscheidungslog: **E-29** (MySQL durchgängig ab T0); E-08 als „DB-Teil überholt" markiert; Abschnitt G
  nachgezogen. **Begründung von E-29 noch von Lucas auszuformulieren (40 % Einzelleistung).**
- Entscheidung: DB-Strategie „MySQL/MariaDB durchgängig, dev = prod via Docker" gewählt (Tradeoff
  Docker-Hürde fürs Anfänger-Team bewusst akzeptiert); GL-geforderte Analyse als voller KI-Entwurf zur Prüfung.
- Commit/Push: Branch `docs/mysql-vorgabe-einarbeiten` → PR auf `main` (Push/PR mit Admin-Freigabe durch Lucas).
- Nächster Schritt: E-29-Begründung ausformulieren; Drift `Tasks+Projektplan.md` P0.1 + Pi-Anleitung
  nachziehen; P0-Scaffolding mit MariaDB-Compose.
- —architekt

## [15:41] architekt · Johannes (docs/erinnerung-update)
- Was/Wo: Session-Abschluss DTB-11. Doku-PR #19 gemergt (Journal + persönl. Entscheidungslog auf `main`). README-Nachtrag: erinnerung-Dateien teamweit editierbar (Admin-Freigabe Lucas). Lokale Altlast-Branches `feat/ci-cd-setup` + `feat/ci-pipeline` entfernt.
- Hinweis: **#20 gemergt — MySQL/MariaDB als verpflichtende Backend-DB** (Stack-Änderung ggü. SQLite/T0); betrifft requirements + CI von DTB-11 (DB-Treiber nötig).
- Entscheidungslog: zwei neue Einträge (#5 erinnerung teamweit editierbar, #6 Ruleset auf `main` beschränken).
- Git/PR: dc8693a "docs(erinnerung): Nachtrag …" auf `docs/erinnerung-update`; CI weiterhin PR #18 (auf Eis bis DTB-1).
- Nächster Schritt: DTB-1 / Scaffolding; DB-Wechsel SQLite→MySQL in CI/requirements/Datenmodell berücksichtigen.
- —architekt
