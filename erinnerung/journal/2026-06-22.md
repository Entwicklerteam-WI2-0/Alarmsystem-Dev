# Journal 2026-06-22

## [14:18] architekt · Johannes (feat/ci-base)
- Was/Wo: CI/CD-Setup (DTB-11) — `.github/workflows/test.yml` + `requirements.txt`/`requirements-dev.txt` (Laufzeit/Dev getrennt, Versionen `~=` gepinnt, pip-Cache, timeout-minutes, permissions: contents read). Drei Review-Runden eingearbeitet (alle LOW/INFO). Branch wird **auf Eis gelegt**: voller Abschluss hängt an DTB-1 (Scaffolding `src/`+Tests), weil die CI sonst nicht grün wird und die DoD-Schutzregel `main` blockieren würde.
- Git/PR: e4b0c32 "ci: Test-Workflow (GitHub Actions) und Requirements fuer T0-Stack" auf `feat/ci-base`; **PR #18** offen & mergebar. PR #15 geschlossen; verwaiste Branches `feat/ci-cd-setup`, `feat/ci-pipeline` am Remote (Cleanup → Lucas).
- Jira: DTB-11 Statuskommentar gesetzt (fertig / offen / Abhängigkeit DTB-1).
- Workflow-Notiz: Ruleset blockt Push-Korrekturen auf Feature-Branches (nur Neuanlage erlaubt) → Empfehlung an Lucas, Ruleset auf `main` zu beschränken.
- Nächster Schritt: DTB-1 / Scaffolding → danach PR #18 mergen (Lucas, §7) + Branch-Protection „Require status checks" (Check `test`) aktivieren.
- —architekt

## [15:39] architekt · docs/mysql-vorgabe-einarbeiten
- Was/Wo: Surprise-Vorgabe der Geschäftsleitung (MySQL-Datenhaltung,
  `02-Arbeitsdokumente/Surprise Anforderungen.txt`) in die Architektur eingearbeitet.
  **Backend-Konzept.md:** §6 Tech-Stack auf **MySQL/MariaDB** (dev = prod via Docker-Compose,
  SQLAlchemy + Repository-Pattern, Alembic) umgestellt; neue **§6a** mit der GL-geforderten Analyse
  (Alternativen/Vor-Nachteile/Auswirkungen/Risiken, als KI-Entwurf markiert); §4 DB-Typen + §7 Struktur
  (`migrations/`, `docker-compose.yml`). **README.md:** Scope, Tech-Stack-Tabelle, Deliverable-Beschreibung,
  Schnelleinstieg (`docker compose up -d db`) gespiegelt.
- Entscheidungslog: **E-29** (MySQL durchgängig ab T0); E-08 als „DB-Teil überholt" markiert; Abschnitt G
  nachgezogen. **Begründung von E-29 noch von Lucas auszuformulieren (40 % Einzelleistung).**
- Entscheidung: DB-Strategie „MySQL/MariaDB durchgängig, dev = prod via Docker" gewählt (Tradeoff
  Docker-Hürde fürs Anfänger-Team bewusst akzeptiert); GL-geforderte Analyse als voller KI-Entwurf zur Prüfung.
- Commit/Push: Branch `docs/mysql-vorgabe-einarbeiten` → PR auf `main` (Push/PR mit Admin-Freigabe durch Lucas).
- Nächster Schritt: E-29-Begründung ausformulieren; Drift `Tasks+Projektplan.md` P0.1 + Pi-Anleitung
  nachziehen; P0-Scaffolding mit MariaDB-Compose.
- —architekt

## [15:41] architekt · Johannes (docs/erinnerung-update)
- Was/Wo: Session-Abschluss DTB-11. Doku-PR #19 gemergt (Journal + persönl. Entscheidungslog auf `main`). README-Nachtrag: erinnerung-Dateien teamweit editierbar (Admin-Freigabe Lucas). Lokale Altlast-Branches `feat/ci-cd-setup` + `feat/ci-pipeline` entfernt.
- Hinweis: **#20 gemergt — MySQL/MariaDB als verpflichtende Backend-DB** (Stack-Änderung ggü. SQLite/T0); betrifft requirements + CI von DTB-11 (DB-Treiber nötig).
- Entscheidungslog: zwei neue Einträge (#5 erinnerung teamweit editierbar, #6 Ruleset auf `main` beschränken).
- Git/PR: dc8693a "docs(erinnerung): Nachtrag …" auf `docs/erinnerung-update`; CI weiterhin PR #18 (auf Eis bis DTB-1).
- Nächster Schritt: DTB-1 / Scaffolding; DB-Wechsel SQLite→MySQL in CI/requirements/Datenmodell berücksichtigen.
- —architekt
## [16:45] backend-db · feat/db-pi-setup (db)
- Was/Wo: **DB-Infrastruktur auf dem Raspberry Pi** eingerichtet (`icedetection.local` / `192.168.1.102`, User `pi`).
  MariaDB **11.8.6** (Debian 13), gehärtet (`mariadb-secure-installation`), **localhost-only** (`127.0.0.1:3306`).
  DB **`vereisung`** (utf8mb4) + App-User **`anr_app@localhost`** (least-privilege, nur `vereisung`).
  NF-07: `.env`-Handling **scoped** in `04-Source-code/source/` (`.env.example` + `.gitignore`), **nicht** root-global.
  **`02-Arbeitsdokumente/Pi-Setup.md`** (DB-Zugang: SSH/VS-Code/DBeaver-Tunnel) ergänzend zur Hosting-Anleitung.
- Git: Branch **`feat/db-pi-setup`**, 3 Dateien **uncommitted** → Commit/Push durch Team (§7, Lucas).
- ⚠️ Divergenz zu §6/E-29: unsere Pi-DB läuft **nativ** (apt MariaDB 11.8), nicht via Docker-Compose — vor Scaffolding abgleichen.
- Nächster Schritt: **Datenmodell (Jira)** — 6 Entitäten + Pydantic-Schemas in `04-Source-code/source/` (vorher Struktur prüfen).
- —database-engineer

## [17:20] architekt · feat/p0-backend-grundgeruest
- Was/Wo: **P0-Grundgerüst gelegt** (P0.2 + P0.3). FastAPI-Skelett `src/main.py` mit `GET /health`
  (Test grün: `tests/test_health.py` → 1 passed, via venv fastapi/httpx/pytest verifiziert). Repo-Struktur
  nach Backend-Konzept §7 (`src/{ingest,model,assessment,storage,api,config,forecast}`, `tests/`,
  `migrations/`, `config/`), `requirements(-dev).txt`, `pyproject.toml` (pytest+ruff), `docker-compose.yml`
  (MariaDB), `.env.example` (nur Platzhalter). Junk-Platzhalter `dfghjk.txt` entfernt.
- Git: Branch **`feat/p0-backend-grundgeruest`**, Commit **80d5dd1** (lokal, **nicht gepusht** — Freigabe Lucas, §7).
- ⚠️ **ABGLEICH NÖTIG — Backend-Repo-Root:** Grundgerüst liegt in **`04-Source-code/`** (direkt). DB-Engineer
  legt `.env`/Datenmodell aber in **`04-Source-code/source/`** ab ([16:45]). Vor Push festlegen, welcher der
  Root ist (`04-Source-code/` vs. `04-Source-code/source/`) — sonst Doppelstruktur. (Architekt-Entscheidung.)
- ⚠️ **DB-Setup-Divergenz:** mein `docker-compose.yml` (MariaDB-Container, §6/E-29) vs. real eingerichtete
  **native** Pi-MariaDB ([16:45]) — vor Storage-Impl abgleichen.
- Beratung (mit Lucas): G1-Seam — **Push / `POST /readings`** empfohlen (Contract-Hoheit, Fail-safe natürlich,
  Multi-Sensor-fähig); G2 baut den Endpoint + liefert Referenz-Sender/Simulator. „G2 baut die Endpoints"
  korrekt, aber **GET ≠ Ingest** (GET serviert raus, POST nimmt rein). Schwellen-Dummies blockieren P0 nicht.
- Tooling: **Kimi repariert** — `@moonshot-ai/kimi-code` global neu installiert (Paketverzeichnis fehlte,
  Wrapper blieben) → `kimi --version` 0.19.0.
- Offen: Branch-Cleanup **pausiert** (PR-Status ausstehend; **Johannes' `feat/ci-base` verschonen**).
- Nächster Schritt: Repo-Root + DB-Divergenz klären → dann Push/PR P0, danach P2 Vertical Slice.
- —architekt

## [18:35] architekt · Jira-Board MySQL-Überarbeitung + README/Fortschrittslog (main)
- Was/Wo: **DTB-Board (Jira) an MySQL-Vorgabe + 04-Source-code angepasst** (Multi-Agent-Workflow Review→Plan→adversarial Verify). **20 Tasks editiert** — Persistenz SQLite→SQLAlchemy/MySQL (DTB-28/12/29/24/34/18), Fail-safe um DB-Ausfall (DTB-43/13/49), Redundanz aufgelöst (Reading-Schema 18↔37, Hysterese 38↔27, Config-Duplikat 15/16), Timing Epic **E-04/DTB-5 M2→M3**. **5 neue Tasks:** DTB-53 docker-compose, DTB-54 Alembic, DTB-55 Engine-Bootstrap, DTB-56 Treiber (PyMySQL), DTB-57 Pi-Retention. **2 Blocks-Links** (15→38, 13→43).
- Owner festgelegt: **Lucas = Assignee** auf Schnittstellen-Naht (DTB-12/19/26/35 = E-02) + Bewertungslogik **DTB-38** (kritischer Pfad, übernimmt Lucas selbst).
- Doku: **README** an reale `04-Source-code/`-Struktur + MySQL-Setup (`pip`/`uvicorn`) angepasst; neuer **`05-Fortschrittslog/`** (Code-Stand + Commit/PR-Log). **PR #29 → main** (b887c11). Repo-Root-Divergenz damit geklärt: Backend-Root = `04-Source-code/`.
- ⚠️ Befund (adversarial Verify): **E-ID-Kollision im Entscheidungslog** — E-29 dreifach (DB + AE-01 + Fail-safe-ADR), E-30 zweifach. Vorschlag: AE-01→E-30, NF-02→E-31, Fail-safe-ADR→E-32 — **im Doc noch aufzulösen (DRI Lucas)**.
- Offen: (1) Jira-Pfad-Präzisierung `04-Source-code/` in Task-Texten + Redundanz-Tasks DTB-53/56 (Gerüst hat docker-compose+PyMySQL schon). (2) E-ID-Auflösung im Entscheidungslog. (3) DTB-16 als Duplikat schließen. (4) `Projektplan-Jira-Backlog-G2.md` noch SQLite-Stand.
- —architekt
