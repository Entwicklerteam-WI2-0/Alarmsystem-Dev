# Journal 2026-06-23

## [00:10] architekt · fix/assessment-kaskade-failsafe -> main (#35)
- Was/Wo: Logik-Review der neuen 3-Faktor-Bewertung (#32, E-32/E-33). Zwei harte Befunde in
  `02-Arbeitsdokumente/Schwellenwerte.md` §2 gefixt: **(1) Klassifikationslücke** `0 °C < T_s ≤ +1,0 °C`
  **mit** Feuchte (`ΔT ≤ 1,0`) → traf keine Stufe → undefiniert/Fail-safe-Verstoß; **(2) ORANGE/ROT-
  Überlappung** ohne dokumentierte Auswertungsreihenfolge. Lösung: §2 als **priorisierte Kaskade**
  (ROT → ORANGE → GELB → GRÜN, erste zutreffende gewinnt, **GELB = Auffang**) + Pseudocode-Vorgabe für
  DTB-38 + **Fail-safe** bei nicht berechenbarem `ΔT` (konservativ Feuchte = wahr → nie GRÜN). **Keine
  Schwellenwerte geändert**, nur Auswertungsstruktur. Beide Vorfälle unverändert aufgelöst.
- Entscheidung: **E-34** im Entscheidungslog (Begründung + verworfene Alternativen).
- Commit/Push: 3c703b0 "fix(assessment): Bewertung als priorisierte Kaskade - Luecke + Fail-safe (E-34) (#35)";
  PR #35 gegen main **gemergt** (war bei Cleanup-Start bereits gemergt, Branch remote gelöscht).
- Jira (DTB): zirkulärer Link **10011** (DTB-14 "blocks" DTB-38) als falsch erkannt — korrekte Abhängigkeit
  ist **DTB-15** (Link 10075, bereits vorhanden). MCP hat **kein** Delete-Tool → DTB-14 + DTB-38
  Beschreibungen mit `----DELETE----`-Marker versehen. **Manuelle Löschung von Link 10011 steht aus**
  (Jira-UI Papierkorb ODER REST `DELETE /rest/api/3/issueLink/10011`).
- Cleanup: lokale verwaiste Branches entfernt (`fix/assessment-kaskade-failsafe`, `feat/agenten-onboarding`);
  `main` fast-forward auf 3c703b0. **PR #18 `feat/ci-base` (Johannes) bewusst verschont.**
- Offen: (1) Jira-Link 10011 manuell löschen. (2) untracked `02-Arbeitsdokumente/Task-Zuweisungsvorschlag-G2.md`
  (nicht vom Architekt angelegt) — Ablage/Commit klären. (3) Hysterese-Begriff „untere Schwelle" in §2
  präzisieren + `assess_ice_risk`-Signatur (RH redundant, da nur `T_d`-Input).
- Nächster Schritt: Link 10011 löschen; dann P0-Push / Vertical Slice (DTB-1 → DTB-8).
- —architekt

## [12:07] architekt · Johannes (feat/config-thresholds-loader)
- Was/Wo: **DTB-15 (Config-Infrastructure: thresholds.json + Loader, P0.5, NF-05)** umgesetzt — Enabler für
  DTB-38. `04-Source-code/config/thresholds.json` (vereisung-Kaskade + Prognoseschwelle, DUMMY-Werte aus
  `Schwellenwerte.md §2`, markiert, DB-frei) + `src/config/loader.py` (`load_thresholds()` → typisiertes,
  `frozen` `Thresholds`; validiert **Struktur + Werte**, scheitert *laut* mit `ConfigError`, kein stiller
  Default) + `tests/test_config_loading.py` (**12 Tests, 100 % Line+Branch-Coverage**, ruff sauber).
- Scope-Korrektur: zwischenzeitlich vorgebaute Abschnitte (`taupunkt_magnus`/`hysterese`/`datenstatus`/
  Prognosehorizont) **wieder entfernt** — gehören zu DTB-32/-27/-18/-33 (in Jira **unzugewiesen**) bzw. sind
  keine Schwellen (Magnus = physikal. Konstanten). „Name = Inhalt".
- Selbstreview (WP5): `quality-gate` · `python-review` (2 Runden, M1/L1–L6 behoben) · `test-coverage` durch.
- Jira: Kommentar an **DTB-11** (Unstimmigkeit `requirements.txt`: `sqlalchemy`/`alembic` vs. E-35-Vorgabe
  „PyMySQL, kein SQLAlchemy" → Klärung mit Lucas vor PR-#18-Merge).
- Entscheidungen: **3 Einträge** im persönlichen `Petzold-Entscheidungslog` (Typmodell, fail-loud Validierung,
  Scope-Begrenzung) — Begründungen vom Autor ausformuliert.
- Commit/Push: b1a60ae "feat(config): thresholds.json + validierender Loader (DTB-15)" (+ 8e1e5f7/e383e78
  docs entscheidungslog); Branch nach `origin` gepusht, **PR gegen `main` offen** (Review Arezo/Amelie, Merge Lucas).
- Offen: (1) DTB-11 requirements-Unstimmigkeit klären (Lucas). (2) vorbestehender ruff-Fehler
  `src/ingest/__init__.py:1` (E501, fremder Scaffolding-Code).
- Nächster Schritt: PR-Review/Merge (Lucas); danach kritischer Pfad DTB-38.
- —architekt

## [14:12] architekt · API-Contract final + Projekt-Briefings + Backlog-Übersicht
- Was/Wo: **G1→G2 API-Vertrag konkretisiert und verankert**:
  - `02-Arbeitsdokumente/Backend-Konzept.md` §9 mit verbindlichem JSON-Contract für G1 `GET /current`
    (Snapshot + `measured_at`, Pflicht-Trias `surface_temp_c`/`air_temp_c`/`humidity_pct`, `GET /health`
    `200`/`503`) und der Verhandlungsposition („`measured_at` + `/health` nicht verhandelbar").
  - `04-Source-code/README.md` mit gleichem Contract-Block ergänzt.
  - `CLAUDE.md` + `AGENTS.md` mit **Mandatory-Read**-Hinweis auf `Backend-Konzept.md` §9 versehen.
- **DTB-15 Review** auf aktuellem `main`: thresholds.json + loader.py + 11 Tests (grün) passen zu
  `Schwellenwerte.md` §2; offen bleibt die spätere Verknüpfung mit DB-Entity `ThresholdSet` (DTB-38 Folge).
- **DTB-1/DTB-51 Status-Check**: P0.3-Code (`src/main.py`, `tests/test_health.py`) ist auf `main` und
  getestet; Jira zeigt DTB-51 noch als „In Arbeit" → Status-Update ausstehend.
- **Desktop-Backlog-Übersicht** erstellt: `C:/Users/luceb/Desktop/DTB-Backlog-Uebersicht.md` (nur lokal,
  nicht im Repo) mit Epic-Hierarchie, persönlicher Prioritätenliste für Lucas und Blocker-Grafik.
- Commit/Push: 57a2c5d "Add API contract FROZEN v1 (draft) (#44)"; Branch `main`, working tree clean.
- Offen: (1) Jira-Status DTB-51 auf „Erledigt" setzen; DTB-53/54/55/56 (DB-Setup) angehen; DTB-35
  Contract an G1/G3 kommunizieren; Jira-Link 10011 löschen.
- Nächster Schritt: DB-Zugang + schema.sql einspielen (DTB-53/54), dann DTB-38 Bewertungsmodul.
- —architekt

## [15:51] backend-dev · Luca Ganter (feat/dtb-19-openapi-v1)
- Was/Wo: **DTB-19 (P1.2) — OpenAPI v1 Spec** geschrieben, formale Abschrift des eingefrorenen
  Contracts (`API_FROZEN_v1.md`). 2 Dateien unter `04-Source-code/docs/api/v1/`:
  `openapi.yaml` (G2-API, 6 Ops: `/v1/health`, `/v1/assessment/current`, `/v1/readings`,
  `/v1/alarms`, `/v1/alarms/stream` SSE, `POST /v1/alarms/{id}/ack`) + `g1-consumed.openapi.yaml`
  (konsumierter G1-Vertrag, 2 Ops). Fail-safe NF-01 (Stale/Fault=`unknown`, nie GRÜN) + RB-01
  (kein Aktor) explizit dokumentiert.
- QS: `openapi-spec-validator` grün (beide Dateien, alle `$ref` lösen auf); **santa-loop**
  (adversariales Dual-Review) durchlaufen → 3 MEDIUM-Befunde eingearbeitet (Fail-safe-Invariante
  + Beispiel `failsafe_fault`; `AlarmSeverity`-Mapping als TBD; `/v1/readings` als T1 markiert).
- Operationszahl: alte DoD „≥ 15 Ops" durch **Architekten-Vorlage** (DRI, Jira DTB-19 Kommentar
  23.06.) überholt → Lean-Set ~6+2 maßgeblich (im Datei-Header dokumentiert).
- Nebenbei: G3-Team-Sync-Fragenkatalog `02-Arbeitsdokumente/Team-Sync-G3-Fragen.md` erstellt
  (PR #43, bereits gemergt).
- Commit/Push: fbadf02 + 778bf40 auf `feat/dtb-19-openapi-v1` (lokal, **noch nicht gepusht** —
  Auth interaktiv durch Luca).
- Offen: (1) Branch pushen + PR. (2) **DTB-26 G3-Bestätigung** (`team-sync-confirmed`) einsammeln —
  **G1-Seite hat Lucas übernommen**. (3) kein `api-v1.0`-Tag vor G1/G3-Sign-off. (4) LOW-Nice-to-haves:
  Health-`enum`, SSE-`$ref`, ack gibt Alarm-State zurück, `driving_factor`-enum.
- Nächster Schritt: Push + PR (DTB-19); dann G3-Sign-off (DTB-26).
- —backenddev

## [16:08] backend-dev · Luca Ganter (feat/dtb-19-openapi-v1)
- Was/Wo: **PR #48 Review-Findings (DTB-19) eingearbeitet** in `openapi.yaml` + `g1-consumed.openapi.yaml`:
  `/v1/alarms/stream` → `503` ergänzt; `POST /v1/alarms/{id}/ack` → `409` bei Double-Ack + Description
  (nicht idempotent, NF-09); `AckRequest.operator` → `minLength: 1`; g1-`status` → Sync-Hinweis auf
  `SensorStatus`. Mit `openapi-spec-validator` erneut geprüft (beide valide); **kein doppelter 503**
  (Web-Fix 865de56 + mein Fix sauber zu einem Block konsolidiert).
- Commit/Push: 18c3648 "docs(api): Review-Findings PR #48 einarbeiten (DTB-19)"; Branch **gepusht (in sync)**.
- Offen: PR #48 mergen lassen (Reviewer/Lucas); DTB-26 G3-Sign-off (`team-sync-confirmed`);
  kein `api-v1.0`-Tag vor G1/G3-Bestätigung.
- Nächster Schritt: PR-#48-Merge abwarten; dann G3-Sign-off (DTB-26).
- —backenddev

## [22:00] architekt · Johannes Petzold (DTB-11 Test-CI + Poller-Fail-safe)
- Was/Wo: **DTB-11 (Test-CI) abgeschlossen** — `.github/workflows/test.yml` neu gegen das `04-Source-code/`-Layout (`pytest --cov` ≥ 80 % + `ruff`), gemergt **#50**; veralteter PR #18/`feat/ci-base` abgelöst & geschlossen. **Branch-Schutzregel „Require status checks" mit Check `test` aktiv.** Python-Matrix 3.12/3.14 + Aggregator-Check `test` (DTB-11b, **#52**, gemeinsam mit Lucas).
- **Poller-Fail-safe-Fix (DTB-12):** beim Selbstreview der CI gefunden — ein nicht-numerisches optionales `pressure_hpa` ließ `poll()` mit ungefangener `ValueError` crashen statt fail-safe `None` (NF-01-Verstoß). Per TDD gefixt + Validierungs-/verdeckte-Pfad-Tests → `poller.py` 100 % Coverage. Im vertieften Review verfeinert (pressure nicht-blockierend, `status=fault`→Ablehnung, UTC-only, `RepositoryError`). Gemergt **#53/#54**.
- Entscheidungen: **8 neue Einträge** im persönlichen `Petzold-Entscheidungslog` (CI-Layout/Branch, ruff-in-CI, CI-DB-Strategie (a), Matrix+Aggregator, Poller-Fix, Muster-Vereinheitlichung, Poller-Härtung/Korrektur, Lokal→kein Multi-Flughafen) — Begründungen vom Autor ausformuliert.
- Commit/Push: #50/#52/#53/#54 auf `main` (`d86e8d6`); lokale Branch-Reste aufgeräumt, Remote sauber. Entscheidungslog-Edit noch lokal/uncommittet.
- Offen: (1) Entscheidungslog-Edit committen/pushen (Doku-PR). (2) DTB-11/DTB-12 Jira-Status (manuell, niedrige Prio). (3) Kritischer Pfad: DB-Setup (DTB-53–56) + Persistenz (DTB-28) → dann Bewertungsmodul DTB-38.
- Nächster Schritt: DB-Setup/Persistenz (DTB-28) bzw. Bewertungslogik (DTB-38).
- —Petzold

## [25.06., ~04:00] — DTB-32 Taupunkt-Funktion + DTB-60 Poller-Taupunkt (backend-dev/Luca)
- **DTB-32 (P2.3) fertig:** reine Funktion `calculate_dew_point` (Magnus a=17,62/b=243,12 aus
  `Schwellenwerte.md` §1) in `src/assessment/utils.py`; Guards (RH∈(0,100], `isfinite`, Magnus-Pol →
  `ValueError`); 20 Tests inkl. Frost-/Negativ-Referenzwerte; Coverage `assessment` 100 %. Branch
  `feat/dtb-32-taupunkt-magnus` **gepusht**.
- **DTB-60 (gestapelt auf DTB-32) fertig:** Poller berechnet + plausibilisiert `dew_point_c`, füllt `Reading`.
  Fail-safe: `ValueError` (RH=0) → `None`; **Ergebnis-Plausibilisierung** `T_d < MIN_TEMP_C` → `None` (schließt
  RH≈0-Gap, sonst stilles GRÜN). 4 neue Poller-Tests, volle Suite **86 grün**, `poller.py` 100 %. Branch
  `feat/dtb-60-poller-taupunkt` **gepusht**.
- Beide via **TDD + santa-loop** (je 2 Prüfer + Moderator; je 1 echter Fail-safe-Blocker gefunden & gefixt).
  Persönl. Entscheidungen (DTB-32 strikter Rechner; DTB-60 Ergebnis-Plausibilisierung) im
  `Ganter-Entscheidungslog` auf `docs/ganter-entscheidungslog-dtb-32` **gepusht**.
- **Neu offen:** (1) **Merge-Reihenfolge:** DTB-32 → `main` zuerst, dann DTB-60 (Base umstellen/rebasen),
  Log-PR unabhängig; PR/Merge = Lucas-Freigabe (§7). (2) **Folge-Ticket:** DTB-38 muss `dew_point_c=None`
  als „Feuchte vorhanden=wahr" behandeln (`Schwellenwerte.md` §2 → nie GRÜN); DTB-12 `dew_point_c: float|None`
  absichern. (3) 3 offene PR-Branches (DTB-32, DTB-60, Entscheidungslog) — Luca hat hier keinen PR-Zugriff.

## [25.06., ~11:10] — DTB-60 (#66) Review + Review-Fixes gepusht (architekt)
- **PR #66 (DTB-60) reviewt** (Code-Review + `python-review`): Magnus-Werte unabhaengig nachgerechnet ✓,
  Fail-safe NF-01 sauber, keine CRITICAL/HIGH. **Review-Fixes verhaltensneutral eingebaut & gepusht**
  (`00de4c9`): `_compute_dew_point()` extrahiert; Konstante `MIN_PLAUSIBLE_DEW_POINT_C`; §3-Begruendung
  praezisiert; Tests WARNING-Level + Grenzwert (strict `<`). **87 gruen, `poller.py` 100 %, ruff sauber.**
- **Neu offen / kritischer Pfad:** (1) Merge-Reihenfolge: DTB-32 (#64) → main, dann #66 rebasen
  (PR/Merge = Lucas-Freigabe). (2) **DTB-38 (#68)** muss `dew_point_c=None` als „Feuchte vorhanden=wahr"
  behandeln (nie GRUEN) — Folge-Abhaengigkeit (heute neue Commits auf #68). (3) Layering ingest→assessment
  (M2, offen, Architekten-Call).

## [25.06., ~12:34] — DTB-28 fertig + PR #70 lokal konfliktfrei (architekt)
- **Backlog-Review** gegen Anfrage-G1.md/G3.md + Team-Sync: CRITICAL-Fix — DTB-58 Stale-Timeout
  war 180 s statt Contract-Wert **120 s** gefixt. **DTB-61** (SSE `GET /v1/alarms/stream`, Petzold) +
  **DTB-62** (`GET /v1/thresholds`, Arash) neu im Backlog.
- **DTB-28 fertig** — `ReadingRepository` auf main's `get_connection()`-Contextmanager portiert. Branch
  `feat/dtb-28-persistenz`, 9 Tests (1 immer / 8 MariaDB-skip). 129 Tests gruen, 94 % Coverage.
  Commit `17c81ce` — **lokal, UNGEPUSHT**.
- **PR #70 Konflikte lokal geloest** (7 Dateien, Branch `fix/pr70-conflicts`, Commit `552d182`) —
  **UNGEPUSHT. GitHub zeigt PR #70 weiterhin als konfliktreich.**
- **Nächster Schritt (DRINGEND):**
  1. Push `fix/pr70-conflicts` → `origin/docs/session-2026-06-25-backend-dev` (PR #70 GitHub-Konflikte).
  2. Push `feat/dtb-28-persistenz` + PR oeffnen.
  3. DTB-28 Jira auf „In Review"; DTB-43 (`GET /v1/assessment/current`) unassigned.
