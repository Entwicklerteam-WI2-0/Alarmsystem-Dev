# Journal 2026-06-28

## [00:04] architekt · feat/cors-g3
- Was/Wo: CORS-Middleware (C1, Strang C) für die G3-Browser-Integration in `src/main.py` + `tests/test_cors.py` + `.env.example`. Env-konfigurierbare Origins (`G2_CORS_ORIGINS`; Default/leer/`*`/Mischform → `["*"]`), `allow_credentials=False`, Methoden `GET/POST/OPTIONS`. Branch `feat/cors-g3` → **PR #127** (offen). Alle Review-Findings aus 3 Runden eingearbeitet: Leerwert-Fallback (MEDIUM), Mischform-Wildcard-Dominanz (LOW), Test-Env-Entkopplung via gezieltem `skipif` + Verdrahtungstest (MEDIUM), expose_headers-Forward-Hinweis (INFO). Entscheidungslog-Eintrag (PR #127, CORS-Strategie) in `Lucas-Entscheidungslog.md`. PR-Body mit DoD/Anforderungs-ID (DTB-23 / NF-07-Bezug) gefüllt.
- Commit/Push: a17a2d3 "docs(cors): expose_headers-Default als Forward-Hinweis dokumentiert" (5 Commits auf `feat/cors-g3`, gepusht)
- Tests: volle Suite **531 passed / 16 skipped**, ruff sauber.
- Nächster Schritt: PR #127 Review/Merge. Strang C weiter — C2 (`servers:`-Eintrag, braucht finale Pi-Adresse), C3 Pi-Deploy (`0.0.0.0`/systemd), C4 DB+G1+Scheduler scharf (G1-Verfügbarkeit offen → ggf. C4a G1-Sim). Zieltermin Pi-Deploy + Live-Test vor Ort **Mo 29.06.**

## [00:04] architekt · Planung (lokal, uncommitted)
- Was/Wo: In `erinnerung/task-prioritaet-nach-audit-2026-06-26.md` neuen **Strang C (Deploy & Live-Integration)** ergänzt (Matrix + Tabelle C1–C6/C4a + Verweise/Fahrplan/Footer). Hintergrund: M2-Kern läuft, aber **nur lokal** (`localhost:8000`, Scheduler AUS); für den Live-Test fehlen Pi-Deploy + Integration. Pi existiert (HS-WLAN, vor Ort bestätigt), G1 vermutlich nur vor Ort/lokal.
- Commit/Push: noch **uncommitted** (`M erinnerung/task-prioritaet-nach-audit-2026-06-26.md`) — bewusst, da parallele Agents liefen.
- Nächster Schritt: Strang-C-Doku + C1-✅ auf einem Doku-Branch committen (nicht auf `feat/cors-g3`, damit der CORS-PR sauber bleibt).

## [03:30] architekt · feat/dtb-24-alarm-ack
- Was/Wo: **DTB-24 (`POST /v1/alarms/{id}/ack`, FA-10) gebaut** — neues `AcknowledgementRepository` (atomare Quittierung: State `active→acknowledged` + `acknowledgement`-INSERT + `alarm_acknowledged`-Audit in EINER `transaction()`, `SELECT … FOR UPDATE` gegen Double-Ack-Race), Endpoint in `src/api/v1.py` (200/400/404/409/422/503 im `Error{code,message}`-Format), `error_response`-Helper + **globaler `RequestValidationError`→422-Handler**, `ack_repo` im DI-Graph (`build_runtime`/`conftest`). **Auth-frei** (Contract-Vorgabe „M2 kein Auth-Header", additiv in M3/DTB-63).
- Commit/Push: `0bee9f4` "feat(api): DTB-24 — POST /v1/alarms/{id}/ack" → **PR #132** (offen). Gebaut via isoliertem `git worktree` (GitHub Desktop hatte den Hauptarbeitsbaum auf `feat/db-finalisierung` zurückgewechselt).
- Tests: 17 neu; volle Suite **648 passed / 16 skipped, 94 % Coverage**, ruff sauber.
- Nächster Schritt: PR #132 Review/Merge (Architekten-Freigabe). Jira DTB-24: irrige Beschreibungs-„Abstimmung" + `blocked by DTB-63`-Link zurücksetzen. ADR E-xx (auth-frei ack / 422-Envelope) im zentralen Log nachtragen.

## [04:27] architekt · feat/db-finalisierung-real-mariadb (DTB-34, lokal/ungepusht)
- Was/Wo: **DTB-34 `GET /v1/readings`** (Messwert-Historie, T1, FA-03) komplett umgesetzt. Repo: neue `get_readings(sensor_id?, start?, end?, limit, order)` auf `Repository`-ABC + InMemory + PyMySQL (`ReadingRepository`), geteilter Validator `_validate_readings_query` (Whitelist `order` / `limit>0` / tz-aware Grenzen); injection-sicher (parametrisierte Werte, gewhitelistete ORDER-Richtung, code-literale Spalten). Endpoint in `src/api/v1.py` gegen die **eingefrorene `openapi.yaml`** (`from`/`to`/`sensor_id`/`limit`[1..1000]/`order`[asc,desc]), contract-konform 400/503 `Error{code,message}` (nie `{detail}`), `Cache-Control: no-store`, RB-01 rein lesend, NF-01 nicht berührt. `bad_request()`-Helper in `responses.py`; additives `503` in `openapi.yaml` für `/v1/readings` (T1). README + `task-prioritaet-aktuell.md` (B3 ⏳ + Status-Update 28.06.) nachgezogen.
- Review: TDD (RED→GREEN). 3-Reviewer-Subagenten-Loop (python/fastapi/security) → **kein CRITICAL/HIGH**, SQL-Injection dreifach ausgeschlossen; MEDIUM/LOW eingearbeitet (json_schema_extra-Schema-Hints fürs `/openapi.json`, defensiver `ValueError`→400, InMemory nutzt Validator-Rückgabe, `params: list[Any]`, `**_kwargs`, Test-Annotations + `limit`-Grenztests). Overseer `uni-review-orchestrator` → **FREIGABEREIF**.
- Tests/Qualität: volle Suite **669 passed / 27 skipped**, ruff check+format clean, `api/v1.py`+`responses.py` **100 %** Cov, `openapi.yaml` valide.
- Commit/Push: **uncommitted/ungepusht** (Working Tree, vermischt mit der DB-Finalisierungs-Arbeit auf `feat/db-finalisierung-real-mariadb`).
- Nächster Schritt (Lucas): DTB-34 vor PR ggf. auf eigenen `feat/dtb-34-*`-Branch trennen → PR/Merge → Jira DTB-34 „Wird überprüft"/„Erledigt". Follow-up (kein DTB-34-Scope): seam-weiter `RequestValidationError`-Handler (FastAPI-Default-422 `{detail}` → `Error{code,message}`).

## [~04:30] backenddev · Autonome Nachtschicht (3 Dev-Zyklen + 4 Merge-Konflikte)
- Was/Wo: Autonomer Lauf (Lucas schlief, /goal autorisierte Push/PR). **Merge-Konflikte gelöst** für #128 (DTB-20), #130 (DTB-63), #132 (DTB-24) — zuvor #131 (DTB-34) reviewt+iteriert (inzwischen in main). **3 komplette Dev-Zyklen** je Plan→TDD→Impl→Quality-Gate→Selbst-Review(Subagent)→PR→unabh. PR-Review(Subagent)→Re-Fix, in isolierten Worktrees (Sonnet):
  - **DTB-49 → PR #135**: `tests/test_failsafe_integration.py`, 6 E-40-Schichten. Unabh. Review HIGH (toter service-Branch) → echter Poller-Fault-Test. 653 passed.
  - **DTB-66 → PR #136**: `driving_factor`/`explanation` befüllt. Unabh. Review fand echten HIGH-Logik-Bug (Forecast-Schwelle ungeprüft → falscher Operatortext) → behoben+verifiziert. 662 passed, Assessment-Cov 100 %.
  - **DTB-68 → PR #134**: ruff-format-Defaults in `pyproject.toml` (Step lag bereits seit #113 vor → Scope verkleinert). Unabh. Review APPROVE.
- Commit/Push: 3 Feature-Branches + Log-Branch `chore/autonomous-cycles-log-2026-06-28`, alle gepusht; PRs #134/#135/#136 offen, alle CI grün + MERGEABLE.
- **Befund (Folge-Ticket):** `check_plausibility()` (Sprung/Flatline, E-40 §3) ist toter Code — im Poller/Service nirgends verdrahtet. NICHT still gefixt.
- Nächster Schritt: menschliche Reviewer-Freigabe + Merge #134/#135/#136 (+#128/#130/#132). Bericht: `~/Desktop/Nachtschicht-Bericht-2026-06-28.md`.
