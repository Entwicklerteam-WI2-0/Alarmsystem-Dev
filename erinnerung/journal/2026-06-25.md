# Journal 2026-06-25

## [00:15] architekt · main
- Was/Wo: G3-Endpoint-Anfrage beantwortet + **API-Contract v1.0 final eingefroren (P1.4, DTB-35)**.
  - G3 fragte nach Endpoint-Liste/Variablen → vollständige G2→G3-Auskunft (6 Endpoints) gegen `openapi.yaml` gegeben; per 3-Agenten-Review (Fakten / Use-Case / Vollständigkeit) verifiziert: feldgenau & RB-01-konform, ergänzt um Praxis-Caveats (unknown am `risk_level` erkennen, **nicht** am `is_stale`; CORS fehlt serverseitig; Fehlercodes 409/404/400/422/503 + `{code,message}`; SSE Heartbeat `:keep-alive`/`Last-Event-ID`; `/v1/alarms` ohne `?state` = nur offene Alarme).
  - **Bereinigte Versandkopie** `Desktop/G2-API-v1-openapi.yaml` erstellt (interne Repo-Pfade / `g1-consumed`-Verweis / OPERATIONSZAHL-DoD-Block raus) — per 3-Agenten-Review bestätigt: valide OpenAPI 3.0.3, `openapi-spec-validator` + `openapi-typescript`-Client-Gen grün, **kein Drift** ggü. `enums.py`/`schemas.py`/`API_FROZEN_v1.md`.
  - **Freeze:** `API_FROZEN_v1.md` → Status **EINGEFROREN (v1.0)** + Freeze-Checkliste + **Bestätigungs-Block** (G1/Nils + G3-Lead, Sign-off 2026-06-23, im Namen der Leads durch Architekt dokumentiert); `openapi.yaml` (Repo + Desktop) `info.version` → 1.0.0. Beide nach Bump erneut validiert (PASS).
- Commit/Push: **KEINER** — Änderungen lokal/ungepusht (`API_FROZEN_v1.md`, `04-Source-code/docs/api/v1/openapi.yaml` modified). main = e867655 "Add seam-sync templates; mark contracts signed".
- Jira: **DTB-35 (P1.4 Contract einfrieren + kommunizieren) → Erledigt** + Abschluss-Kommentar. DTB-19 (API-Spec) und DTB-26 (Seam-Sync) waren bereits Erledigt.
- Nächster Schritt: Git-Tag `api-v1.0` + P1.4-Commit (Freigabe Lucas ausstehend); G3-Lead-Name nachtragen; Versandkopie + Kurznachricht an G3; **kritischer Pfad M2:** Backend bedient `/v1` noch nicht (nur `/health`, ohne Präfix) → `/v1`-Router + CORS + Fail-safe in `assessment/` (noch leer) bauen.

## [03:55] backend-dev · Luca (feat/dtb-32 + feat/dtb-60)
- Was/Wo: **DTB-32** (Taupunkt-Funktion `calculate_dew_point`, Magnus a=17,62/b=243,12 aus
  `Schwellenwerte.md` §1) und darauf gestapelt **DTB-60** (Poller berechnet + plausibilisiert `dew_point_c`,
  fuellt `Reading`). Beide per TDD (RED→GREEN) + je `santa-loop` (2 Pruefer + Moderator).
  - DTB-32: Guards (RH-Bereich, `isfinite`, Magnus-Pol); santa-loop-Fixes (stilles NaN→GRUEN, RH=0-Naht).
    20 Tests, Coverage `assessment` 100 %.
  - DTB-60: `ValueError`→`dew_point_c=None` gefangen (Fail-safe-Auflage aus DTB-32); santa-loop-Blocker
    **H1** (RH≈0 → absurder Taupunkt) via Ergebnis-Plausibilisierung (`T_d < MIN_TEMP_C` → None) geschlossen.
    4 neue Poller-Tests, volle Suite 86 gruen, Coverage `poller.py` 100 %.
- Commit/Push: `5e48579`+`34ad0cd` (DTB-32), `b0c7364`+`4e9d7c1` (DTB-60). Alle Feature-Branches **gepusht**.
  Entscheidungslog (DTB-32 + DTB-60) auf `docs/ganter-entscheidungslog-dtb-32` (`44b76c7`) gepusht.
- Naechster Schritt: (1) **Merge-Reihenfolge** morgen: DTB-32 → `main` zuerst, dann DTB-60 (Base auf `main`
  umstellen/rebasen), Log-PR unabhaengig. (2) **PR/Merge = Lucas-Freigabe** (§7). (3) Folge-Ticket: DTB-38
  muss `dew_point_c=None` als „Feuchte vorhanden=wahr" behandeln (`Schwellenwerte.md` §2 → nie GRUEN);
  DTB-12 `dew_point_c: float|None` absichern.
