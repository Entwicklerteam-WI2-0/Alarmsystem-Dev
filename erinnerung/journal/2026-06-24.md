# Journal 2026-06-24

## [15:45] backend-db · Leon (feat/dtb-54-schema-apply)
- Was/Wo: **DTB-54** (schema.sql gegen MariaDB einspielen, ersetzt Alembic). Apply-Schritt + DB-Rechte
  dokumentiert: README-Abschnitt „Schema & DB-Rechte einspielen" (PowerShell **und** cmd, Rollen root/alarm
  getrennt, Pi-SSH-Tunnel, Verifikation inkl. Negativ-Test) + neue **`migrations/grants.sql`**: append-only
  (NF-09) via **GRANT statt Trigger**; `alarm` behält UPDATE für `alarm`-state-Übergänge. Vorab `santa-loop`
  (2 Prüfer + Moderator): 4 CRITICAL gefixt (Branch-Hygiene, kumulative GRANT-Semantik, PowerShell-`<`,
  DoD-Nachweis).
- Commit/Push: `d2aa4bc` „docs(db): Apply-Schritt + append-only-Grants fuer schema.sql (DTB-54)" — **lokal,
  NICHT gepusht** (Freigabe-Gate §7). Branch `feat/dtb-54-schema-apply` (sauber von `main`).
- Nächster Schritt: (1) Init-Schritt `CREATE DATABASE`+`CREATE USER` noch ins README (von-null-Lücke).
  (2) DoD-Apply-Beweis (2× apply + `COUNT(*)=6` + Negativ-Test) bei **Pi-MariaDB-Init** nachholen — bis dahin
  DTB-54 „Wird überprüft", nicht verifiziert-done. (3) PR erst nach Lucas-Freigabe. (4) persönl. Entscheidung
  (append-only via GRANT; Apply ohne Skript) noch in `Hartling-Entscheidungslog` (liegt auf `feat/dtb-55`).
