---
description: Session-Start — Kontext, Stand und Regeln laden (Resume)
---
Du beginnst eine Arbeitssitzung in diesem Repo. Antworte auf **Deutsch**.

Gehe so vor:
1. **Erinnerung lesen:** alle Dateien in `erinnerung/` (v. a. `erinnerung/stand.md`) — aktueller Stand, offene Punkte, letzte Entscheidungen.
2. **Regeln laden:** `CLAUDE.md`. Verinnerliche besonders: **RB-01** (kein Freigabe-/Aktor-Endpoint), **Fail-safe** (bei Ausfall/Stale nie GRÜN), **Bewertungslogik nur aus `Schwellenwerte.md`**, **kein direkter `main`-Push**.
3. **Git-Stand prüfen:** aktueller Branch und ob der Remote voraus ist. Hinkt der Branch hinterher, **schlage `git pull` vor** — führe ihn nur nach Bestätigung aus (kein automatisches Mergen, keine destruktiven Aktionen).
4. **Zusammenfassen (5–8 Zeilen):** Wo stehen wir? Was ist als Nächstes dran? Welche Regeln gelten für die anstehende Aufgabe?

Erfinde **keine** Fakten — alles aus den Dateien (Source of Truth siehe `CLAUDE.md`).
