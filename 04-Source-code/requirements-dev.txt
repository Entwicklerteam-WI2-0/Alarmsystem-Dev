# Entwicklungs-/Test-Abhängigkeiten (zusätzlich zu requirements.txt).
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
pytest-dotenv>=0.5.2  # laedt .env automatisch fuer pytest (DB-Integrationstests liefen sonst still-skippen,
                     # weil die App os.environ liest, s. docs/live-test-runbook.md §1c). CI-sicher via
                     # override=False (s. pyproject.toml): eine vorhandene Prozess-Env gewinnt gegen .env.
httpx>=0.27
ruff==0.15.18  # exakt gepinnt: `ruff format` ist versionsabhaengig -> lokal == CI (Format-Gate stabil, kein spontanes Rot durch ruff-Update)
pyyaml>=6.0  # Contract-Guard test_contract_enum_sync.py liest die OpenAPI-YAMLs
